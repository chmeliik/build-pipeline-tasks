# container-build-catalog

- `task/` - tasks used in the Konflux container build pipeline
- `pipelines/` - the pipelines that use said tasks
  - The [`hack/generate-pipelines.sh`](hack/generate-pipelines.sh) script generates most of these.
    See [pipeline-generator/README.md](pipeline-generator/README.md) for more details.
